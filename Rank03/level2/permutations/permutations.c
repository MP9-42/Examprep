#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int position;
int len;



void permute(char *str)
{
    int len = strlen(str);

}

int main(int argc, char **argv)
{
    if (argc == 2)
        permute(argv[1]);
    return(0);
}
